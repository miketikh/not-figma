# Copy to .env.local

# Firebase Client Configuration (for browser/client-side)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_DATABASE_URL=

# Firebase Admin SDK (for server-side API routes - REQUIRED for AI features)
# Get these from Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# Extract the values from the downloaded JSON file and add them here:
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=
# Note: For the private key, keep the quotes and \n characters (e.g., "-----BEGIN PRIVATE KEY-----\nYour key...\n-----END PRIVATE KEY-----\n")
# In Vercel/deployment: Add these as environment variables in your hosting platform

# AI Configuration
# OpenAI API Key for AI Assistant feature
# Get your API key from: https://platform.openai.com/api-keys
# Requires an OpenAI account with billing enabled
# Cost: ~$0.001-0.005 per command using GPT-3.5-turbo
OPENAI_API_KEY=

# Optional: Anthropic Claude API (not currently used)
# ANTHROPIC_API_KEY=

# Langfuse Observability (Optional - for AI monitoring/debugging)
# Langfuse provides detailed tracing, token usage tracking, and debugging for AI features
# Sign up at https://cloud.langfuse.com or https://us.cloud.langfuse.com to get your API keys
# Free tier: 50,000 observations/month (enough for ~5,000-10,000 AI commands), 90 days retention
# Docs: https://langfuse.com/docs/integrations/frameworks/vercel-ai-sdk
LANGFUSE_SECRET_KEY=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_BASEURL=https://cloud.langfuse.com
